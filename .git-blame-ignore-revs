# 이 목록의 커밋은 git blame 에서 건너뛴다 — 내용이 아니라 서식만 바꾼 커밋들이다.
#
# 사용법(한 번만 설정하면 로컬 blame·IDE 가 자동으로 따른다):
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub 웹 blame 은 이 파일을 자동으로 인식한다.
#
# 서식만 바꾼 커밋을 새로 만들면 여기에 SHA 를 한 줄 추가한다.

# style: Prettier 전면 적용 (165 파일)
86fb2beb26c07ff5bc79e9143f16c175ee225ec1
