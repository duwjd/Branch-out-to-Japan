---
title: ② 마케팅 스튜디오 · 상세페이지 UI 기획
space: 화면 스펙
status: 초안
phase: Phase 0
updated: 2026-08-21
owner:
tags: [UI기획, 스튜디오, 상세페이지]
---

# ② 마케팅 스튜디오 · 상세페이지 UI 기획

| **작성일** | **작성자** | **비고** |
| --- | --- | --- |
| 2026.08.10 | @Calia Kim | 최초 작성. 화면 2개(생성 퍼널 + 결과) · 생성 퍼널 안에 블록 구성 확인 단계 신설. `02-studio-ui-기획서.md` HOME-01b의 "상세페이지 전환 준비 중" 배지 해제 |
| 2026.08.11 | @Calia Kim | **1차 사용 피드백 반영 개정 3건** — (1) 템플릿 카드에 구성 미리보기 추가(DETAIL-04) (2) 생성물 테마 신설(DETAIL-04b·BRAND-11) (3) 밴드 리듬 도입으로 "슬라이드 나열" 해소(RESULT-D03). 인터랙티브 프로토 `3-detail-home.html`·`4-detail-confirm.html`·`5-detail-result.html` + 공용 엔진 `detail-proto.js` |
| 2026.08.11 | @Calia Kim | **2차 개정 2건(앱 구현 반영)** — (4) **한국어 입력 자동 변환** 신설: 폼을 한국어로 받고 콜⑧이 일본 표기로 옮기며 확인 단계(CONFIRM-06)에서 사용자가 검토·수정한다. DETAIL-05 설명문·DETAIL-07 안내 개정 (5) **DETAIL-04 미리보기를 실제 생성물로 교체** — `npm run detail:previews` 가 실 파이프라인으로 구운 `public/detail-templates/preview-D{1..6}.webp` |

# 0. 개요

상세페이지 스튜디오는 제품컷(과 한국 상세페이지 원본)을 입력받아, 상세페이지를 구성하는 **블록을 하나씩 이미지로 만들고 세로로 이어붙인** 일본향 상세페이지를 생성하는 기능이다. 범용 상세페이지 빌더가 아니다 — 블록 카탈로그 27종과 템플릿 6종 안에서만 조립하고, 근거를 입력하지 않은 배지·가격·수치는 만들지 X(`../02-detail-converter-spec.md` §0 관통 원칙).

**입력한 사실만 렌더한다**는 썸네일 화면의 문법을 그대로 계승한다. 다만 상세페이지에는 그 문법의 결과가 훨씬 눈에 띄게 나타난다 — 근거가 없으면 그 **블록이 통째로 사라지기** 때문이다. 그래서 이 화면은 썸네일에 없던 규칙을 하나 더 갖는다: **빠진 블록과 그 사유를 접지 않고 노출한다**(CONFIRM-02). "수상 배지 블록 — 실적 3필드 중 집계일이 비어 있어 넣지 않았습니다"처럼 무엇을 채우면 살아나는지까지 말한다. 제외는 실패가 아니라 기본 동작이고, 사용자가 그것을 납득해야 이 서비스의 안전장치가 신뢰로 바뀐다.

이 기능의 구조적 차별점은 **문자를 AI가 그리지 않는다**는 것이다. AI는 글자 없는 배경 사진만 만들고, 캐치카피를 포함한 모든 문자는 코드가 폰트로 그린다. 썸네일에서는 "오탈자 없음"이 프롬프트 부탁이었지만 여기서는 사실이다 — 상세페이지 한 블록에 200~600자가 들어가고 `※効能評価試験済み` 같은 근거 표기는 한 글자만 어긋나도 근거로서 무효가 되기 때문이다. 화면은 이 사실을 검수 게이트(RESULT-D02)에서 "모든 문자를 벡터 폰트로 렌더 — AI 이미지 생성이 글자에 개입하지 않는다"로 말한다.

화면은 **상세페이지 홈(생성)**과 **생성 결과 상세** 2개다. 다만 생성 홈은 2단계로 나뉜다 — **입력 → 블록 구성 확인**. 시퀀스가 10~16블록이라 무엇이 들어가는지 모른 채 생성하면 건당 약 2분·$0.42가 헛돌기 때문이다. 확인 단계를 별도 라우트로 빼지 않고 같은 페이지의 단계로 둔 이유는 업로드한 파일을 다시 받지 않기 위해서다. 확인 단계는 부작용 없는 미리보기 라우트(`POST /api/studio/detail/plan`)를 호출하므로, 되돌아가도 서버에 쓰레기 파일이 남지 X.

생성 로딩은 별도 화면이 아니라 **결과 화면의 시작 상태**다(썸네일과 동일). 다만 진행 표시가 다르다 — 블록이 10~16개라 단계 문자열 하나로는 부족해서 **"블록 7/14"** 진행률과 블록별 점등 리스트를 함께 보여준다.

**출력이 2종**이라는 점도 화면이 반드시 설명해야 한다. 라쿠텐 R-Cabinet은 이미지 1장당 3840px·2MB 제한이라 "이어붙인 긴 1장"은 **업로드 자체가 물리적으로 불가능**하다. 결합본은 확인·승인·공유용이고, 몰에 실제로 올라가는 것은 분할본이다. RESULT-D04는 두 버튼을 나란히 두고 이 관계를 한 줄로 말한다.

데이터 계약 델타(`../../08-data-flow.md` §6): (1) `GeneratedAsset.kind` 에 `'detail'` 추가 (2) `detailInput`(제출 스냅샷 — 상품종류·스펙·성분·옵션·근거·프로모·업로드 경로·끈 블록) (3) `blockTotal`/`blockDone` 비정규화 카운터 (4) `slicePaths` (5) **`asset_blocks` 테이블 신설** — 블록별 status·slots·imagePath·height·version·history. jsonb 컬럼이 아니라 행 단위인 이유는 AI 블록이 동시에 완료돼 read-modify-write 로는 진행률이 유실되기 때문이다. `image_path` 는 결합본 fileId로 **재사용**하므로 라이브러리·다운로드 코드가 썸네일과 같은 경로를 쓴다.

## 0-1. 2026-08-11 개정 3건 — 무엇이 문제였고 무엇을 바꾸는가

**(1) 템플릿을 고를 근거가 텍스트뿐이었다.** 카드가 블록명 불릿 6개와 "외 N개"만 보여줬다. 상세페이지는 순서가 곧 설득 구조인데, 그 순서를 글자로만 읽고 상상해야 했다. 그래서 카드에 **구성 미리보기**를 넣는다. 정적 이미지가 아니라 `blockSequence`에서 그린 도식이라 (a) 시퀀스를 고치면 미리보기가 자동으로 따라오고 (b) 아래에서 테마를 바꾸면 6장이 그 색으로 다시 칠해진다. 실측 참고 컷(`assets/templates/`)은 타사 저작물이라 **제품 UI에 쓰지 X**.

**(2) 어느 브랜드로 만들어도 결과물 색이 같았다.** 팩의 `moodProfiles` accent가 6종 전부 `#ff6464`(YOAKE 자사 코랄)였고 블록 렌더 토큰도 코랄 고정이었다. 고객 브랜드 정체성이 산출물에 없고, 우리 브랜드색이 고객 상세페이지로 새어 나갔다. 그래서 **생성물 테마**를 신설한다. 기본값은 **업로드한 제품 대표컷에서 뽑은 색**이고, 브랜드 관리(BRAND-11)에 저장해 두면 그 값이 기본이 되며, 생성할 때 건별로 덮어쓸 수 있다. 해석된 값은 자산에 스냅샷되므로 나중에 테마를 바꿔도 이미 발행한 자산의 블록 재생성은 흔들리지 X.

> **경계** — 테마는 **생성 산출물에만** 적용된다. YOAKE 앱 화면의 코랄 디자인시스템(`design/design-system.md`)은 그대로다. 로고 색을 UI 토큰으로 승격하지 않는 것과 같은 원칙이다.

**(3) 상세페이지가 아니라 슬라이드를 이어 붙인 것으로 읽혔다.** 27블록 중 24개가 같은 틀(전방위 여백 72px → 코랄 eyebrow → 헤드라인)이었고, 사진 블록은 "사진 위 흰 라운드 카드"에 전면 흰 스크림까지 겹쳐 있었으며, 모든 사진 높이가 1500px 고정이었다. 블록 하나하나의 문제가 아니라 **블록들 사이에 관계가 없다**는 문제였다. 그래서 **밴드 리듬**을 도입한다 — 톤 교대 · 풀블리드 사진 + 하단 스크림 · 높이 3프리셋 · 디스플레이 타이포 · 챕터 레일. 계약 정본은 `../02-detail-converter-spec.md` §2-6.

## 0-2. 2026-08-11 2차 개정 2건 (앱 구현 반영)

**(4) 한국어로 입력하면 그 블록이 조용히 사라졌다.** 폼 라벨은 전부 한국어인데 placeholder 만 일본어 예시였고, `주의사항`·`리뷰`·`시험 조건`·`집계 기간`·`GIFT`·`프로모 각주`는 placeholder 조차 없었다. 그런데 이 값들은 렌더러가 자단위로 그리는데 JP 폰트에 한글 글리프가 없어, 한국어가 들어오면 글자가 깨지는 게 아니라 **블록이 통째로 빠졌다**(`hero-product` 면 필수 블록이 증발). 사용자는 결과물을 보고서야 알았다.

그래서 **입력을 한국어로 받는다.** 콜⑧이 일본 표기로 옮기고, 확인 단계(CONFIRM-06)가 원문과 나란히 보여주며 사용자가 고칠 수 있다. 숫자·가격은 원문과 대조해 한 자리라도 달라지면 채택하지 X. 「추가 요청」만 **영어로** 옮긴다 — 이미지 생성 프롬프트 나머지가 전부 영어라 언어를 섞으면 지시가 흐려진다. 계약 정본은 `../02-detail-converter-spec.md` §2-9.

> **경계** — 이건 콜⑦(카피 재설계)과 성격이 정반대다. 카피는 다시 쓰고, 사실 필드는 **값을 그대로 둔 채 표기만** 옮긴다. 서비스의 한 줄 정의("번역이 아니라 재설계")는 카피에 대한 말이고, 근거 필드까지 재설계하면 그건 표시 사실의 변조다.

**(5) 템플릿 미리보기가 도식이었다.** 1차 개정에서 `blockSequence` 로 그린 도식을 넣었는데, 도식은 "무엇이 몇 번째로 오는가"만 말하고 "그래서 어떻게 보이는가"는 말하지 못한다. 그래서 **실제로 구운 결과물**로 바꾼다 — `npm run detail:previews` 가 6종을 실 파이프라인으로 생성해 `public/detail-templates/` 에 굽는다. 제품컷은 실존 제품이 아닌 **가공 이미지**(`assets/samples/haruon-mock-product.png`)를 쓴다.

앱 셸은 ⓪ MAIN-01 정본을 따른다. 모듈 탭에서 "상세페이지"가 활성화되고, 사이드바 "마케팅 스튜디오"는 `/app/studio` 하위 전체에서 활성 상태다. **비회원 열람 + 실행 직전 게이트**(2026-07-23)를 따른다 — 비회원도 입력·구성 확인까지 할 수 있고, "상세페이지 생성"을 누르는 순간에만 로그인 모달이 뜬다. 내용 정본은 `../02-detail-converter-spec.md`(파이프라인 7단계·검수 게이트), 템플릿·블록 정의는 `../../research/jp-detail-style-taxonomy.md`, 슬롯 계약은 `data/processed/detail-style-prompts.json`(v1.0.0)이다.

---

# 1. 상세페이지 홈(생성)

| **이미지** | **번호** | **내용** |
| --- | --- | --- |
|  | DETAIL-00 | **진입**<br> - 사이드바 "마케팅 스튜디오" → 썸네일 홈 → 모듈 탭 "상세페이지"<br> - ③ 자산 라이브러리 "상세페이지" 탭 빈 상태 링크<br> - 홈 최근 자산 카드(상세페이지는 결과 화면으로 직행)<br> - 라우트 `/app/studio/detail` |
|  | DETAIL-01 | **페이지 헤더·모듈 탭**<br>1a. 헤더 — "마케팅 스튜디오 / 상세페이지 만들기" + 한 줄 설명 "블록을 하나씩 만들어 세로로 이어 붙입니다. 글자는 전부 폰트로 그리므로 일본어가 깨지지 않습니다."<br>1b. 모듈 탭 3개 — [썸네일(링크) / **상세페이지(활성)** / 인스타 피드(준비 중)]. 구 기획서의 "상세페이지 전환 준비 중" 배지는 해제한다 |
|  | DETAIL-02 | **원본 이미지 (필수)**<br>2a. 드롭존 + 파일 선택. JPG·PNG·WebP · 10MB · **최대 10장**<br>2b. 업로드 순서 = 상세페이지 위→아래 순서. 썸네일 그리드에 **순번 배지**를 찍고 개별 제거(×) 가능<br>2c. 안내 — "제품컷 1장은 필수입니다. 한국 상세페이지 원본을 위→아래 순서로 함께 올리면 갭 진단에 씁니다."<br>2d. (추후 기획) 브랜드 자산에서 선택 — 썸네일 HOME-02의 2탭 구조를 상세에도 도입 |
|  | DETAIL-02b | **테마 색 추출 (2026-08-11 신설)**<br>2b-1. 첫 장 업로드 즉시 그 이미지에서 브랜드 색과 무드를 뽑아 DETAIL-04b의 기본값으로 넣는다. AI 콜 0 · 서버 왕복 0<br>2b-2. 결과 카드 — 스와치 + "○○의 지배색 #4AB3A6 · 유효 픽셀 14% · 무드 미니멀 클린" + [바꾸기] 링크. **색이 어디서 왔는지 화면이 말한다**<br>2b-3. **입력은 반드시 첫 장(제품 대표컷)만.** 2번째 장부터는 한국 상세페이지 원본이라 한국어 UI 색(빨강 세일 배너 등)이 섞여 결과를 오염시킨다. 이 제약은 코드로 못박는다<br>2b-4. 유효 픽셀이 3% 미만이면(무채색 패키지 등) 상품 종류 기본 팔레트로 대체하고 그 사실을 문구로 밝힌다<br>2b-5. 첫 장 썸네일에 "색 추출" 태그를 얹어 어느 이미지가 기준인지 보이게 한다<br>2b-6. 알고리즘 정본은 `../02-detail-converter-spec.md` §2-7 |
|  | DETAIL-03 | **상품 종류 · 타깃 플랫폼 (필수)**<br>3a. 상품 종류 칩 6종 — 스킨케어/선케어/색조/클렌징/헤어/기타. **템플릿 기본값과 이미지 무드를 결정한다**<br>3b. 타깃 플랫폼 칩 — 미정/아마존JP/라쿠텐 공식샵/라쿠텐 리셀러/Qoo10. 출력 폭·분할 규격도 여기서 갈린다<br>3c. **아마존JP 선택 시 경고 배너** — "아마존JP A+ 콘텐츠는 가격·프로모션 표기가 규정상 금지라, 프로모션 블록은 넣지 않습니다." 동시에 DETAIL-06b(프로모션 입력) 영역 자체를 **숨긴다** |
|  | DETAIL-04 | **템플릿 (필수·1개) — 2026-08-11 개정**<br>4a. 카드 6종 2열 그리드. 각 카드는 **구성 미리보기(좌) + 이름·설명·블록명(우)** 좌우 배치<br>4b. **구성 미리보기(2026-08-11 2차 개정 — 실제 생성물)** — 그 템플릿을 **실 파이프라인으로 돌려 만든 결과물**(`public/detail-templates/preview-D*.webp`). 폭 74px 스트립으로 상단만 보여주고 하단은 마스크로 페이드해 "페이지가 이어진다"를 표현. 좌하단 [전체 보기] → 모달에서 전체 스크롤<br>4c. 프리뷰는 `npm run detail:previews` 로 다시 굽는다. 도식이 아니라 산출물이므로 **템플릿 정의를 바꾸면 반드시 다시 구워야 한다**. 파일이 없거나 로드에 실패하면 블록명 목록만으로 폴백한다(카드가 깨지지 X)<br>4d. 목록은 **템플릿 고유 시퀀스** 앞 4개를 쓴다. 프로모·실적 레이어는 조건부이고 6장 모두 같아서, 그걸 보여주면 카드가 서로 구분되지 X<br>4e. **"추천" 배지는 상품 종류 + 플랫폼이 둘 다 맞을 때만.** 플랫폼만 보면 라쿠텐에서 6장 전부에 배지가 붙어 아무것도 구분하지 못한다(팩 `selectionGuide.byCategory`와 같은 판정축)<br>4f. 메타 한 줄 — "블록 N개 · 이미지 생성 N개". 이 수치는 조건부 레이어까지 펼친 **실제 생성될 시퀀스** 기준<br>4g. 고지 1줄 — "미리보기는 데모 입력으로 실제 생성한 결과이고, 제품컷은 실존 제품이 아닌 가상 브랜드용 이미지입니다. 실제 산출물은 입력하신 내용과 이미지로 만들어집니다."<br>4j. 프리뷰는 **프로모·실적 레이어를 끄고** 굽는다. 그 레이어는 6종 공통으로 앞에 4블록이 붙어 카드 상단이 전부 같아진다 — 템플릿을 구분해 보여준다는 목적을 스스로 무너뜨린다<br>4h. 내부 ID(D1~D6)는 화면에 노출하지 X — 라벨은 "문제해결 서사형" 같은 평문(라벨 정책 계승)<br>4i. 실측 참고 컷(`assets/templates/`·`public/studio-templates/`)은 **타사 저작물이라 쓰지 X** — 해당 README 사용 조건 3 |
|  | DETAIL-04b | **테마 (선택 · 이번 건만) — 2026-08-11 신설**<br>4b-1. 기본값 = 브랜드 관리(BRAND-11)에 저장된 테마. 없으면 **업로드한 제품 대표컷에서 추출**한 색<br>4b-2. 요약 줄 — 스와치 + `#4AB3A6 · 미니멀 클린` + 출처(`제품컷에서 추출 · 파일명` / `로즈 코랄 프리셋` / `직접 입력`) + **대비 배지**("○ 본문 대비 4.7:1 · 강조 밴드 5.2:1")<br>4b-3. 오버라이드 — 프리셋 팔레트 10종 칩 + 무드 8종 칩 + 직접 입력(color picker + hex). 고르면 템플릿 미리보기 6장이 즉시 다시 칠해진다<br>4b-4. [제품컷 색으로 되돌리기] — 오버라이드를 풀고 추출값으로 복귀<br>4b-5. **여기서 바꿔도 브랜드 설정은 그대로다.** 이번 생성 건에만 적용되고 자산에 스냅샷된다. 이 문장을 섹션 설명에 그대로 노출<br>4b-6. 브랜드에 테마가 없으면 안내 — "아직 제품컷이 없어 상품 종류 기본색을 씁니다" + 브랜드 관리 링크<br>4b-7. 접이식 "이 테마가 이미지 생성 프롬프트에 어떻게 들어가는지 보기" — `{{moodKeywords}}`·`{{surfaceColor}}`·`{{accentColor}}` 치환 결과를 그대로 보여준다. 색을 고르는 행위가 **AI 프롬프트를 바꾸는 행위**임을 화면이 말한다<br>4b-8. 팔레트·무드는 `role="radiogroup"` + 방향키 순회. 선택은 색만이 아니라 **✓ 기호**로도 표시(CVD 방침) |
|  | DETAIL-05 | **제품 스펙 (필수)**<br>5a. 内容量 · 区分 · 販売元 · 原産国 · 全成分<br>5b. **고지(2026-08-11 2차 개정)** — "표시 의무 항목입니다. 내용을 고쳐 쓰지 않고, 한국어로 입력하시면 일본 표기로만 바꿔 넣습니다 — 바꾼 결과는 다음 단계에서 확인·수정하실 수 있습니다."<br>5b-1. 구 문구("입력하신 원문을 그대로 넣습니다")는 변환 도입으로 **거짓이 되므로 반드시 교체한다.** 화면이 하지 않는다고 말한 일을 하는 것이 가장 나쁜 실패다<br>5b-2. `区分` 은 LLM 이 아니라 결정적 매핑표가 옮긴다(화장품→化粧品 · 의약외품→医薬部外品). 코드가 `'医薬部外品'` 문자열 비교로 히어로 블록의 기능 라벨을 켜므로, 표기가 한 글자만 달라도 라벨이 조용히 사라진다<br>5b-3. `全成分` 은 변환하되 CONFIRM-06 에서 **기본 펼침 + 표시 의무 경고**로 노출하고, 검수 게이트에도 "업로드 전 실제 표기와 대조" 문구가 남는다<br>5c. 内容量·区分·販売元 셋 중 하나라도 비면 서버가 400으로 차단(약기법 표시 의무) |
|  | DETAIL-05b | **성분 · 무첨가 · 사용법 (선택)**<br>5b-1. 성분 — `성분명\|농도\|배합목적` 줄 입력. **미입력 시 안내**: "성분을 입력하지 않으면 성분·기전 블록은 넣지 않습니다. 성분명을 지어내지 않습니다."<br>5b-2. 무첨가 항목 · 스펙 수치(`라벨\|값`) · 사용법 STEP · 주의사항 |
|  | DETAIL-06 | **실적 · 시험 근거 (선택 · 접이식)**<br>6a. 힌트 라벨 — "그룹별로 전부 채워야 해당 블록이 들어갑니다"<br>6b. 랭킹 3필드(실적명·부문·집계일) / 누적 판매 2필드(수치·집계기간) / 시험 5필드(시험명·조건·기관·시점·n수) / 리뷰 원문(`본문\|평점\|연령대`)<br>6c. 리뷰 라벨에 "실제 리뷰만 넣습니다" 명시 — 후기 생성 금지 |
|  | DETAIL-06b | **프로모션 (조건부 · 접이식)**<br>6b-1. 세트명·판매가(필수 쌍) / 통상가·할인율·증정품·한정 조건·각주<br>6b-2. **통상가 실적 확인 체크박스** — "통상가로 실제 판매한 실적이 있습니다. (체크하지 않으면 통상가 취소선을 넣지 않습니다 — 有利誤認 방지)". 썸네일 HOME-05b 문법 그대로 계승<br>6b-3. 아마존JP 선택 시 이 섹션을 **렌더하지 않는다**. 우회 제출은 서버가 400으로 차단 |
|  | DETAIL-06c | **옵션 (조건부 · 접이식)**<br>6c-1. 옵션 축 칩 — 색상/용량·사이즈/세트·수량/종류<br>6c-2. 옵션 목록 — `이름\|색상값\|품번` 줄 입력<br>6c-3. 힌트 — "2개 이상이면 옵션 블록이 들어갑니다". 색상 6개 이상이면 컬러 차트가 추가로 붙는다<br>6c-4. 모델컷 업로드 + **사용 권한 동의 체크** — "미체크 시 해당 블록만 빠지고 생성은 계속됩니다"(썸네일 F와 다른 점: 썸네일은 생성 자체가 불가했다) |
|  | DETAIL-07 | **추가 요청 (선택)**<br>7a. 자유 입력 1줄 — "예: 전체적으로 더 밝고 화사하게"<br>7b. **범위 고지** — "이미지 분위기에 대한 요청만 반영합니다. 근거가 필요한 값(가격·실적·성분)은 위 항목으로만 들어갑니다." 내부적으로는 AI 배경컷 프롬프트의 art direction 으로만 붙고, 제약·금지 문구가 그 **뒤에** 와서 우선순위를 뺏기지 X<br>7c. **언어 안내(2026-08-11 2차 개정)** — "한국어로 쓰셔도 됩니다 — 이미지 생성 모델에는 영어로 바꿔 전달합니다." 일본어가 아니라 **영어**인 이유는 프롬프트 나머지가 전부 영어이기 때문이다. 한국어 원문은 자산의 `promptUsed` 에 그대로 남아 화면이 사용자 입력을 되비춘다<br>7d. 변환에 실패하면(한글 잔존) 지시를 **통째로 버린다** — 한국어를 영어 프롬프트에 섞는 것보다 없는 편이 낫다 |
|  | DETAIL-08 | **하단 sticky 액션 바 · 기타**<br>8a. 전폭 primary "블록 구성 확인" + 안내문(부족한 입력을 우선순위대로 1개만 말한다)<br>8b. 재설계 고지 — "번역이 아니라 **일본 고객 관점의 메시지 재설계**입니다. 근거를 입력하지 않은 배지·가격·수치는 만들지 않습니다."<br>8c. 클라이언트·서버 검증 이중 적용 — 버튼 잠금을 우회해도 `lib/server/detailForm.ts` 가 차단<br>8d. 브랜드 전환 시 작성 중 입력 경고 대상에 이 경로 포함(AppShell)<br>8e. (미정) 최근 생성 스트립 — 썸네일은 2026-07-22에 제거했으므로 상세에도 두지 X |

---

# 2. 블록 구성 확인 (생성 홈 2단계)

> 썸네일에 없던 단계다. 시퀀스가 10~16블록이라 "무엇이 들어가고 무엇이 왜 빠졌는지"를 생성 **전에** 확인시킨다.

| **이미지** | **번호** | **내용** |
| --- | --- | --- |
|  | CONFIRM-01 | **요약 줄 — 2026-08-11 개정**<br>1a. "약 N분 걸립니다 · 블록 N개 (이미지 생성 N개) · 출력 폭 N px · 총 N px → 분할 N장"<br>1b. **테마 줄 신설** — 스와치 + `#4AB3A6 · 미니멀 클린` + "밴드 톤 paper 9 · tint 5 · accent 1" + "테마 바꾸기"(→ DETAIL-04b 딥링크)<br>1c. 플랫폼 출력 안내 — "라쿠텐은 이미지 1장당 3840px·2MB 제한이라, 분할본을 R-Cabinet에 올립니다."<br>1d. 우측 상단 "입력으로 돌아가기" |
|  | CONFIRM-02 | **시퀀스 타임라인 — 2026-08-11 개정**<br>2a. 세로 번호 리스트. 각 행 = 순번 + 블록명(평문) + 배지<br>2b. 배지 — "이미지 생성"(AI 블록) / **"챕터 N/M"**(챕터 오프너) / **밴드 톤**(기본·틴트·강조·다크) / **높이**(`hero 2000px` 등) / **"풀블리드"**(사진 위 카피)<br>2c. 톤 배지는 **색 + 한글 라벨** 2중 표기 — 색만으로 구분하지 X<br>2d. 필수 블록은 "필수" 텍스트, 그 외는 [빼기] 토글. 뺀 블록은 목록 아래 칩으로 모아 [＋ 블록명]으로 되돌린다<br>2e. **톤은 순서 전체를 보고 정해진다** — 블록을 빼면 남은 블록의 톤이 다시 계산된다. 이 문장을 목록 위에 안내로 노출하고, 실제로 재배치되는 것을 보여준다<br>2f. 토글하면 서버 미리보기를 다시 호출한다 — **화면이 직접 추론하지 않는다**(생성 결과와 어긋나지 않게) |
|  | CONFIRM-03 | **빠진 블록 · 사유 (접지 않는다)**<br>3a. 제목 "빠진 블록 N개" + 한 줄 원칙 — "근거가 없으면 그 블록을 넣지 않는 것이 기본 동작입니다. 입력을 채우면 다시 들어갑니다."<br>3b. 카드마다 블록명 + 사유 + **필요한 입력**(예: "성분 데이터", "실적 근거")<br>3c. 사유 예 — "실적 3필드(실적명·부문·집계일)가 모두 채워지지 않아 넣지 않았습니다" / "아마존JP A+ 콘텐츠는 가격·프로모션 표기가 규정상 금지라 넣지 않았습니다" / "이미지 생성 블록 상한(4개)을 넘어 넣지 않았습니다"<br>3d. (추후 기획) 사유 카드에서 해당 입력 필드로 딥링크 |
|  | CONFIRM-04 | **하단 액션 바**<br>4a. [입력 수정] secondary + [상세페이지 생성 (블록 N개)] primary<br>4b. 생성 클릭 = 실행 직전 게이트 지점 — 비회원이면 여기서 로그인 모달 |
|  | CONFIRM-05 | **(추후 기획) 루브릭 커버리지 미터** — 이 시퀀스가 `jp-detail-message-patterns` A~E 18항목 중 몇 개를 덮는지. ① 진단 리포트와 같은 축으로 표시해 "① 진단이 감점한 항목 = ② 생성기가 넣을 블록" 대응을 화면에서 잇는다 |
|  | CONFIRM-06 | **일본어 변환 확인 (2026-08-11 2차 개정 신설)**<br>6a. **한글 입력이 없으면 이 카드는 뜨지 X.** 일본어로 입력한 사용자에게는 LLM 콜도 0회다<br>6b. 헤더 — "일본어 변환 확인" + 배지(전부 통과면 `N개 변환됨`, 실패가 있으면 `확인 필요 N`)<br>6c. 항목마다 **원문(읽기 전용) 위에 변환값(편집 가능한 입력)**. 사용자가 고친 값이 그대로 제출된다<br>6d. **표시 의무 항목(区分·全成分)과 검사 실패 항목은 접지 않는다.** 접힌 채 넘어가면 확인 단계가 무의미하다. 나머지는 [나머지 N개 펼쳐서 확인하기]로 접는다<br>6e. 배지 — `표시 의무 — 반드시 확인`(regulated) / `이미지 지시 · 영어`(추가 요청) / `브랜드 등록 표기`(용어집이 결정) / `확인 필요`(검사 실패)<br>6f. 실패 사유를 그대로 노출 — "숫자가 원문과 다릅니다(원문 …). 값이 바뀌면 표시 위반이 됩니다." / "한글이 남아 있습니다 — 직접 일본어로 입력해 주세요."<br>6g. 사용자가 고친 값도 **서버가 쓰는 것과 같은 검사 함수**로 즉시 재검사한다. 화면의 경고와 서버의 채택 여부가 갈리면 사용자가 고칠 수 없다<br>6h. 상태는 색만이 아니라 **테두리 + 배지 문자 + 사유 문장** 3중 표기(CVD 방침)<br>6i. 블록 [빼기] 토글로 이 화면이 다시 계산돼도 **재번역하지 X** — 변환값을 원문과 함께 서버로 보내 대조하고, 그 사이 입력이 바뀐 경우에만 다시 부른다<br>6j. 변환 자체가 실패하면(모델 오류) 구성 미리보기는 살리고 경고 카드만 띄운다 — 여기서 500을 내면 사용자가 블록 구성조차 못 본다 |

---

# 3. 생성 결과 상세

| **이미지** | **번호** | **내용** |
| --- | --- | --- |
|  | RESULT-D00 | **진입·라우트** — 제출 직후 `/app/studio/detail/{assetId}` 로 이동해 생성중 상태로 시작. 라이브러리·홈 카드에서도 이 경로로 들어온다 |
|  | RESULT-D01 | **생성중 상태**<br>1a. 제목 "상세페이지를 만들고 있어요" + 단계 라벨(`DETAIL_STAGE_LABELS`)<br>1b. **블록 진행률 바** + "블록 7/14". blocks 단계 전에는 불확정 바(IndetBar)<br>1c. 블록 점등 리스트 — 완료(초록)/생성중(코랄)/대기(회색)/실패(빨강)<br>1d. "이 화면을 닫아도 생성은 계속됩니다."<br>1e. 폴링 2.5초. 자산이 done 이어도 **블록이 generating 이면 계속 폴링**(블록 재생성 대응) |
|  | RESULT-D02 | **완료 헤더 · 검수 게이트**<br>2a. 템플릿명 + 배지 — 데모 모드(목) / 검수 통과·확인 필요<br>2b. 검수 게이트 항목 — 오탈자·유령 글자 없음 / 제품 라벨 보존 / 무단 배지·수치 없음 / **각주 정합(고아 각주 0)** + 조건부(가격 입력값 그대로 · 스펙표 원문 그대로 · 길이 상한 적용)<br>2c. 각 항목에 근거 문장을 함께 노출 — "모든 문자를 벡터 폰트로 렌더 — AI 이미지 생성이 글자에 개입하지 않는다"<br>2d. **2026-08-11 신설 2항목** — ① `brandKitApplied` "브랜드 금지 표현 미사용": 브랜드 관리에 등록한 금지 표현과 동의어가 어느 슬롯에도 쓰이지 않았는지 ② `sourceReuse` "보조 이미지 출처": 텍스트 블록에 얹은 보조 이미지는 이번에 생성한 배경컷과 제품 대표컷만 쓴다(한국 상세페이지 원본은 한글이 박혀 있어 코드가 영구 차단)<br>2e. 이 2항목은 **`passed` 판정에 넣지 X** — 법적 게이트가 아니라 브랜드 규칙이라 `degradedBlocks`와 같은 등급이다. 위반해도 생성을 막지 않고 어느 블록인지만 남긴다 |
|  | RESULT-D03 | **결합본 미리보기 (좌측 sticky) — 2026-08-11 개정**<br>3a. 최대 높이 70vh 스크롤 컨테이너 안에 결합본 전체를 세로로 표시<br>3b. lg 이상에서 sticky — 오른쪽 해설을 읽는 동안 이미지가 따라온다<br>3c. 미리보기가 **밴드 리듬을 그대로 보여준다** — 톤 교대 · 풀블리드 사진 + 하단 스크림 위 흰 카피 · 디스플레이 타이포 · 챕터 레일 · 높이 변주<br>3d. 상단 모드 토글 안내 — 현재 구성을 한 줄로("톤 paper 9 · tint 5 · 사진은 풀블리드 + 하단 스크림 · 높이 3프리셋")<br>3e. **프로토 한정** — `[슬라이드형(현행)] ⇄ [밴드 리듬형(개정)]` 토글을 둔다. 같은 시퀀스·같은 카피를 두 렌더러에 태워 개정 근거를 대조시키는 장치이고, **제품에는 넣지 X**(합의되면 밴드 리듬만 남는다) |
|  | RESULT-D04 | **다운로드 (2종)**<br>4a. primary "결합본 1장 내려받기" — 확인·공유용<br>4b. secondary "몰 업로드용 분할본 N장 내려받기" — 순차 다운로드<br>4c. **관계 설명 1줄** — "결합본은 확인·공유용입니다. 라쿠텐은 이미지 1장당 크기 제한이 있어 **분할본**을 올리셔야 합니다."<br>4d. 파일명 `{브랜드명}-상세페이지-{플랫폼}[-demo].jpg` / 분할본은 `-01`, `-02` 접미 |
|  | RESULT-D05 | **블록 리스트 · 블록 단위 재생성**<br>5a. 블록마다 썸네일(상단 정렬 크롭) + 블록명 + 역할 + 배지(이미지 생성/다시 만드는 중/실패/vN) + **밴드 톤·높이 배지**(2026-08-11) — 왜 이 블록이 이 색·이 크기로 나왔는지를 목록에서 바로 읽게 한다<br>5b. [이미지만 다시] — AI 블록만. 카피는 그대로 두고 배경컷만 재생성($0.06)<br>5c. [이전 버전으로] — history 가 있을 때만<br>5d. 안내 — "문자만 있는 블록은 무료로 즉시 다시 그려집니다"<br>5e. 재생성 완료 시 **자동 재결합**(sharp만 도므로 3~4초)<br>5f. (추후 기획) 블록 슬롯 직접 편집 UI — API(`PATCH .../blocks/{id}`)는 이미 있다 |
|  | RESULT-D06 | **재설계 해설**<br>6a. "이 구성을 고른 이유" — 콜⑦ narrativeReason<br>6b. "한국 원본에서 무엇을 바꿨는지" — krElementMap(유지·정제/재설계/제거)<br>6c. (추후 기획) copySlots KR→JP 칩 + 근거 태그 — 썸네일 RESULT-02b 패턴 계승 |
|  | RESULT-D07 | **실패 상태 · 부분 실패**<br>7a. 전체 실패 — 오류 카드 + [다시 시도]<br>7b. **부분 실패** — "블록 N개가 실패해 빠진 채로 이어붙였습니다. 아래에서 개별로 다시 만들 수 있어요."<br>7c. 스테일 가드 — generating 이 10분을 넘으면 폴링 시점에 실패 전환 |
|  | RESULT-D08 | **보조 액션** — [다른 상세페이지 만들기] / [자산 라이브러리] |

---

# 4. 인터랙티브 프로토 (2026-08-11)

개정 3건은 정적 와이어프레임으로는 검토가 안 된다 — "테마를 바꾸면 어떻게 되는가", "블록을 빼면 톤이 어떻게 재배치되는가", "슬라이드형과 밴드형이 실제로 얼마나 다른가"가 전부 **동작**이기 때문이다. 그래서 와이어프레임 3종을 조작 가능한 프로토로 만들었다. 빌드가 없으므로 파일을 브라우저로 열면 바로 돈다.

| 파일 | 확인할 것 |
| --- | --- |
| `3-detail-home.html` | 드롭존에 이미지를 올리거나 데모 제품컷(민트 튜브·앰버 드로퍼·플럼 컴팩트)을 누르면 **실제로 색을 추출**한다. 팔레트·무드를 바꾸면 템플릿 미리보기 6장과 대비 배지가 즉시 갱신된다 |
| `4-detail-confirm.html` | 3에서 고른 테마가 이어진다. [빼기]를 누르면 **톤이 재계산**된다 |
| `5-detail-result.html` | `[슬라이드형] ⇄ [밴드 리듬형]` 토글로 개정 전후를 같은 화면에서 대조한다 |
| `../04-operations/3-brand.html` | BRAND-11 카드 → 연필 → BRAND-11d 모달. 제품 이미지에서 색을 뽑고 미리보기·대비를 확인한 뒤 저장 |

**공용 엔진 `detail-proto.js`** — 색 추출·테마 파생·톤 리듬·미니어처를 4개 화면이 공유한다. 화면은 추론하지 않고 이 엔진의 결과를 그리기만 한다(실제 구현에서 서버 미리보기를 호출하는 자리와 같다). ⚠ **이 파일의 상수·판정식은 `../02-detail-converter-spec.md` §2-6·§2-7과 글자 그대로 같아야 한다** — 프로토가 곧 엔진 스펙이므로, 값을 바꾸면 문서도 같이 바꾼다.

데모 제품컷을 파일이 아니라 캔버스에 그리는 이유: `file://`로 열면 `<img>`를 캔버스에 그리는 순간 오리진이 오염돼 `getImageData`가 막힌다. 캔버스로 그린 픽셀과 `FileReader`의 data URL은 오염되지 않으므로, 이 두 경로만 쓰면 **서버 없이도 추출 알고리즘이 진짜로 실행된다.**
