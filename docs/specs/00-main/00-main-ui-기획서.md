# ⓪ 메인(앱 홈) UI 기획

| **작성일** | **작성자** | **비고** |
| --- | --- | --- |
| 2026.07.20 | @Calia Kim | 최초 작성 |
| 2026.07.20 | @Calia Kim | [수정] 하이파이 리디자인 — 전역 내비 상단바 → 사이드바 셸, 자산 요약 스탯 행 → 사이드바 KPI 위젯, 다음 단계 패널 → 본문 중앙 히어로, 3축 진입 카드 → 사이드바 내비 통합, 진행중 작업 → 우하단 플로팅 패널, 최근 자산 리스트 → 프리뷰 카드 그리드. ①·② 앱 화면도 동일 사이드바 셸로 정합 |
| 2026.07.20 | @Calia Kim | [수정] MAIN-00 비로그인 리다이렉트·MAIN-01e 계정 메뉴 — 로그인·마이페이지 화면 신설(`../03-account/03-account-ui-기획서.md`)에 따라 "(미정)" 해제 및 실화면 연결 |
| 2026.07.20 | @Calia Kim | [수정] MAIN-01c·04c 운영 메뉴 활성화("준비 중" 비활성 → "라이브러리 우선 △" + ③ 운영 홈 이동), MAIN-09 ③ 운영 문구 갱신 — ③ 운영 UI 기획 착수(`../04-operations/04-operations-ui-기획서.md`)에 따름 |
| 2026.07.21 | @Calia Kim | [추가] MAIN-01c 운영 하위 메뉴 델타 — 운영 활성 시 [자산 라이브러리/브랜드 관리/기업 매칭] 아코디언 펼침, 타 축 활성 시 접힘. 상세 정본 ③ LIB-00. ⓪ 프로토타입 무변경 |
| 2026.07.21 | @Calia Kim | [수정] IA 개편 반영 — MAIN-03 썸네일 CTA를 ② 스튜디오 홈(생성)으로, MAIN-05b 썸네일 카드 재열람을 ③ 자산 상세로, "전체 보기"를 ③ 자산 라이브러리 확정 동선으로 (근거: `../../07-ia.md` §5 · ②/③ 기획서 IA 개편) |

# 0. 개요

메인(앱 홈)은 로그인·온보딩(브랜드 프로필 등록) 후 진입하는 앱 홈이자 3축 허브 화면이다. 브랜드 프로필 컨텍스트 위에서 브랜드 자산 요약 · 진행중 작업 · 다음 단계 CTA 3요소를 한 화면에 모으고(`../../07-ia.md` §4), ① 진단 리포트 · ② 마케팅 스튜디오 · ③ 운영으로 가는 진입점이 된다.

셸은 좌측 사이드바 + 본문이다. 사이드바가 브랜드 스위처·3축 내비·KPI 위젯·품의 PDF·계정을 담고, 본문은 다음 단계 히어로와 최근 자산 그리드만 남긴다 — 진행중 작업은 우하단 플로팅 패널로 분리한다. 이 사이드바 셸은 앱 전역 공통이며 ①·② 앱 화면도 동일 셸을 쓴다(07-ia §5 "사이드/탭에 3축"). 화면은 메인 1개로 구성하고, 별도 화면 대신 상태 변형(첫 방문·기본·진행중·로딩·오류)이 화면 구성을 바꾼다.

설계 원칙은 지표 나열형 대시보드가 아니라 "다음 단계 제안"이다 — 3축 성숙도가 서로 달라(① 확정 v4 · ② 썸네일만 확정 · ③ 방향만) 사용자가 다음에 무엇을 할지 모르는 상태가 기본값이기 때문이다. 유사 서비스 7종 조사와 적용 결정은 `../../research/main-dashboard-ui-references.md`. 화면에는 시스템 카운트·발행 산출물만 출력하고 시장 성과 수치·리뷰 등 근거 없는 데이터는 출력하지 않는다(증거 원칙).

이 화면의 진행중 작업 표시는 ② 기획서가 제안한 데이터 계약 델타(`GeneratedAsset.status` 또는 잡 조회, `../02-studio/02-studio-ui-기획서.md` # 0)를 전제한다. 리포트 처리 상태는 `DiagnosisRequest.status`를 읽는다(`../../08-data-flow.md` §7).

PC 기준으로 설계하며 모바일은 반응형 대응만 한다(사이드바 → 상단 블록 스택). 온보딩, 3축 내부 화면(①·② 기획서), ③ 운영 상세는 이번 범위에서 제외한다. 로그인·마이페이지는 별도 기획서(`../03-account/03-account-ui-기획서.md`)에서 다루고, 이 화면은 사이드바 계정 메뉴로 연결만 한다. 상위 구조 정본은 `../../07-ia.md` §4·§5, 화면↔데이터 매핑은 `../../08-data-flow.md` §7이다. 구세대 명세 `design/wireframes/app-spec.md`의 요소 인벤토리를 계승하되, 본 문서가 화면 동작·상태·예외처리의 정본이 된다.

# 1. 메인(앱 홈)

| **이미지** | **번호** | **내용** |
| --- | --- | --- |
|  | MAIN-00 | **진입** - 로그인 성공 시 `/app`으로 리다이렉트되어 진입<br> - 온보딩(브랜드 프로필 등록) 완료 시 진입<br> - 앱 어느 화면에서든 사이드바 워드마크·"대시보드" 클릭 시 진입<br> - URL 직접 접근 시 동일 화면 출력<br> - 비로그인 접근 시 로그인으로 리다이렉트 (`../03-account/03-account-ui-기획서.md` LOGIN-00) |
|  | MAIN-01 | **전역 내비(사이드바)** - 모든 앱 화면에 고정 노출됨. 폭 248px. 좁은 화면에서 상단 블록으로 스택<br>1a. 워드마크<br> - "KGLOW." 클릭 시 대시보드로 이동<br>1b. 브랜드 프로필 스위처 (워드마크 바로 아래 · 전폭 버튼)<br> - 현재 작업 브랜드명 + 카테고리("스킨케어 / <span lang="ja">スキンケア</span>" — 택소노미 정본 표기) 출력<br> - 클릭 시 드롭다운(listbox) 출력: 보유 브랜드 목록 + 현재 브랜드 ✓ + "브랜드 추가"<br> - 브랜드 선택 시 페이지 이탈 없이 홈 데이터가 해당 브랜드로 전환됨 + 토스트 "{브랜드명}(으)로 전환했습니다"<br> - 바깥 클릭·Esc 시 닫힘. `aria-haspopup="listbox"`·`aria-expanded` 적용<br> - 다중 브랜드 지원 범위(1개 전제 vs 다수)는 (미정) — 프로토타입은 2개 데모<br>1c. 3축 내비 (세로 메뉴 · 아이콘 + 라벨 + 성숙도 배지)<br> - "대시보드 / 진단 리포트 / 마케팅 스튜디오 / 운영" 4메뉴<br> - 현재 위치는 코랄 틴트 배경 하이라이트. 아이콘은 스트로크 1.75 인라인 SVG로 통일<br> - 성숙도 배지를 메뉴 우측에 상시 출력 (MAIN-04)<br> - 운영은 "라이브러리 우선 △" 배지 (warning). 클릭 시 ③ 운영 홈(자산 라이브러리)으로 이동 (`../04-operations/04-operations-ui-기획서.md` LIB-00)<br> - 운영 활성 시 하위 메뉴 [자산 라이브러리 / 브랜드 관리 / 기업 매칭] 아코디언 펼침 — 상세 정본 ③ LIB-00 (07.21 개편). 타 축 활성 시 접힘 — 기존 마크업과 동일, ⓪ 프로토타입 무변경<br>1d. 품의용 PDF (사이드바 하단 고정 영역)<br> - "품의용 PDF" 전폭 secondary 버튼 - 최근 발행 리포트의 보고용 산출물 재다운로드 (품의형 결재 동선)<br> - 발행 리포트 0건 시 비노출<br>1e. 계정 (사이드바 최하단)<br> - 아바타 + 이름 + 이메일 행. 클릭 시 마이페이지·로그아웃 메뉴 출력<br> - "마이페이지" 클릭 시 마이페이지로 이동, "로그아웃" 클릭 시 세션 종료 + 로그인으로 이동 (`../03-account/03-account-ui-기획서.md` MYPAGE-00·MYPAGE-07a)<br> - 결제/구독 상태는 마이페이지에서만 표기함. 사이드바에 플랜 배지 출력 X<br> - 알림 벨은 미노출 (추후 기획 — 결정된 것처럼 그리지 X) |
|  | MAIN-02 | **브랜드 자산 요약 (사이드바 KPI 위젯)** - 3축 내비 아래·하단 고정 영역 위에 배치<br> - 소제목 "브랜드 자산" + 4행: "진단 리포트 {n}건" / "생성 썸네일 {n}건" / "최근 진단 점수 {score}/100" / "다음 메가와리 D-{n}"<br> - 값은 시스템 카운트·발행 리포트 산출만 출력. 매출·전환 등 시장 성과 수치 출력 X (실측 연동 없음)<br> - 최근 진단 점수: brand 모드 리포트만 있으면 "—" 출력 (종합점수 없음 — ① 기획서 v4)<br> - 다음 메가와리 D-day는 시즌 캘린더(메가와리 3·6·9·11월) 기반<br> - 로드 시 숫자 카운트업 모션 (400ms). `prefers-reduced-motion` 시 즉시 표시<br> - 항목 클릭 동작 X (자산 목록 화면 미설계 — 추후 기획)<br> - `app-spec.md`의 "매칭" 타일은 ③ 준비 중이라 제외하고 메가와리 D-day로 대체함<br> - 첫 방문(자산 0) 시: 4행 대신 "아직 자산이 없습니다. 첫 진단에서 시작됩니다" 출력 (MAIN-06) |
|  | MAIN-03 | **다음 단계 히어로 (본문 중앙)** - 본문 상단 중앙 정렬. 규칙 기반 추천 1개만 출력<br> - 구성: 캡슐 배지 "{브랜드명} · 다음 단계" → 헤드라인(강조어만 코랄 솔리드) → 설명 1줄 → primary CTA + secondary<br> - 리포트 있음 + 썸네일 없음: "진단에서 재설계한 문구를 썸네일로 만들 차례예요" + "썸네일 만들기" primary → ② 스튜디오 홈(생성) (리포트 블록8 퍼널과 동일 동선) + "리포트 다시 보기" secondary<br> - 리포트 없음: 히어로 미출력 — 첫 방문 셋업 가이드(MAIN-06)가 역할을 대신함<br> - 썸네일 있음: 추천 규칙 (미정 — 재점검·추가 템플릿 중 우선순위 추후 기획)<br> - 추천은 1개만 — 복수 CTA 병렬 출력 X (primary 단일 원칙)<br> - 헤드라인은 화면당 1개의 디스플레이 타이포 (그라디언트 텍스트 사용 X) |
|  | MAIN-04 | **3축 진입 (사이드바 내비 통합)** - 별도 진입 카드 없음 — 사이드바 3축 내비(1c)가 진입점이다<br> - 성숙도 배지는 실제 개발 상태와 일치시킴 — 색+글자+기호 3중 표기<br>4a. 진단 리포트 — "이용 가능 ○" (success). 클릭 시 ① 진단 입력으로 이동<br>4b. 마케팅 스튜디오 — "썸네일 우선 △" (warning). 클릭 시 ② 스튜디오 홈(생성)으로 이동 — ② 홈은 갤러리가 아니라 생성 화면임<br> - 상세페이지 전환·인스타 피드는 배지에 표기 X (스튜디오 홈 탭에서 "준비 중" 노출 — HOME-01b와 동일함)<br>4c. 운영 — "라이브러리 우선 △" (warning). 클릭 시 ③ 운영 홈으로 이동. 배지는 성숙도 실상과 일치함(라이브러리=조회 화면 확정 · 매칭=라이트 · 성과 판별=준비 중)<br> - 축별 최근 활동 표시는 최근 자산 그리드(MAIN-05)가 대신함 |
|  | MAIN-05 | **진행중 작업·최근 자산**<br>5a. 플로팅 진행 패널 (우하단 고정 · 진행 중 잡이 있을 때만 출력)<br> - 헤더: 진행 스피너 + "생성 중 {n}건" + 접기 토글(`aria-expanded`). 접힘 시 헤더만 남음<br> - 항목 행: 미니 shimmer 썸네일 + 이름·타깃 + 단계 텍스트 + 진행바({진행 단계}/{전체 단계} 비율 · 코랄)<br> - 단계 텍스트는 ② 파이프라인 고객어 5종과 동일함 (② RESULT-06)<br> - 완료 시 항목 행이 완료 행으로 전환됨: "썸네일이 완성되었습니다" + 검수 통과 요약 + "보러 가기" → ② 생성 결과 상세. {n}초 후 패널 자동 소멸<br> - 별도 완료 토스트 출력 X — 완료 피드백은 패널에 통합함. 토스트는 브랜드 전환 피드백 전용<br> - ① 리포트 처리중도 같은 패널에 행으로 집계됨 (축 횡단)<br> - 상태 폴링 2.5초 간격 (① PROCESS-01 문법). 터미널 상태 도달·화면 이탈 시 폴링 정지<br> - `role="status"` `aria-live="polite"`<br>5b. 최근 자산 그리드 (히어로 아래)<br> - 프리뷰 카드 3열 그리드. 최신순 고정. "{n}건 · 최신순" 카운트 출력<br> - 카드 = 프리뷰(리포트: 표지 목업 — eyebrow·점수·문서 라인 / 썸네일: 생성물 프리뷰) + 축 칩(① 리포트/② 스튜디오) + 이름 + 생성일<br> - 리포트 카드 클릭 시 ① 리포트 뷰로 이동. 썸네일 카드 클릭 시 ③ 자산 상세로 이동 — 재열람 정본은 ③ (`../04-operations/04-operations-ui-기획서.md` DETAIL-00). hover·키보드 포커스 시 -2px 리프트 + 코랄 보더<br> - 생성 완료 시 새 카드가 그리드 맨 앞에 crossfade로 추가됨 + KPI 썸네일 건수 갱신<br> - 0건이면 영역 자체 미출력. 빈 그리드 출력 X<br> - "전체 자산 보기" 링크 → ③ 운영 자산 라이브러리 (LIB-00). 표시 개수는 (미정) |
|  | MAIN-06 | **첫 방문 빈 상태 (자산 0)** - 히어로·최근 자산 그리드 대신 셋업 가이드를 본문 중앙에 출력<br> - 구성: 캡슐 배지 "시작하기" → 헤드라인 "일본 진출, 3단계로 시작하세요" → 진행 카운터 "{완료}/3 단계 완료" → 체크리스트 카드<br> - 1 브랜드 프로필 등록 — 온보딩 완료로 ✓ 처리됨<br> - 2 첫 진단 리포트 받기 — "진단 시작" primary → 진단 입력<br> - 3 첫 일본향 썸네일 만들기 — 리포트 발행 전 비활성 + "진단 후 열립니다"<br> - 단계 완료 시 체크 전환 + 카운터 갱신<br> - 보조 링크 "샘플 리포트 먼저 보기" — 실물 가치를 먼저 보여줌. 무료=샘플 재정의(v4)에 따른 문안·동선은 (미정)<br> - 사이드바 KPI 위젯은 "아직 자산 없음" 상태로 전환 (MAIN-02). 품의용 PDF 비노출 (MAIN-01d) |
|  | MAIN-07 | **로딩** - 데이터 로드 동안 히어로·그리드 치수를 유지한 스켈레톤 출력<br> - 완료 시 콘텐츠로 전환. 레이아웃 이동 X<br> - 사이드바는 즉시 출력 |
|  | MAIN-08 | **로드 오류** - 대시보드 로드 실패 시 본문 중앙 "대시보드를 불러오지 못했습니다" + "다시 시도" 버튼 출력<br> - 사이드바는 유지. "다시 시도" 클릭 시 재요청 + 로딩 상태(MAIN-07) 출력 |
|  | MAIN-09 | **기타** - 라우트 `/app` (09-dev-spec §2). 3축 라우트는 기존 정의와 동일함 (② HOME-08)<br> - 사이드바 셸은 앱 전역 공통 — ①·② 앱 화면도 동일 셸 적용 (각 기획서 [수정] 참조). 플로우 맵(index.html)은 문서 페이지라 제외<br> - 데모 상태 스위처(검토 스트립의 첫 방문/기본/생성중/로딩/오류 토글)는 프로토타입 검토 전용 — 제품 UI 아님<br> - 알림(벨 아이콘·알림 센터) (추후 기획)<br> - 모바일 앱 셸(햄버거·하단 탭) (미정) — 반응형은 사이드바 상단 스택 + 다단 → 1단만 대응<br> - ③ 운영 UI 기획 착수 (2026-07-20, `../04-operations/`) — 자산 라이브러리(운영 홈 겸)·기업 매칭. 코드 구현은 ③ 주간(7/25~31)<br> - 접근성: 시맨틱 `nav`·`main`, 스위처 listbox aria, 상태 변화 `aria-live`, 전 인터랙션 `:focus-visible` 링 |
