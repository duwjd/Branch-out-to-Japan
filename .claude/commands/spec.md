---
description: 아이디어/기능을 PRD 항목으로 구체화 (pm-planner 위임)
argument-hint: <기획할 기능/아이디어>
---

`pm-planner` 에이전트를 사용해 다음을 PRD 항목으로 구체화한다:

**대상:** $ARGUMENTS

요구사항:
- `docs/00-positioning.md` 의 포지션과 금지선을 기준으로 판단한다.
- 문제 → 사용자 → 해결 → 범위(포함/제외) → 완료 기준 → 성공 지표 구조로 정리한다.
- 불확실한 점은 추측하지 말고 `docs/05-open-questions.md` 에 열린 질문으로 추가 제안한다.
- 결과를 `docs/02-product-spec.md` 에 반영(제안)하고, 무엇을 왜 바꿨는지 요약한다.
