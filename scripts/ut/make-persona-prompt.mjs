/**
 * 페르소나 평가 프롬프트 생성 — 트랙 A(P6) · 트랙 B(P7).
 *
 * 페르소나 에이전트는 도구가 `Read` 뿐이라 디렉터리를 나열하지 못한다. 파일명을 프롬프트에
 * 그대로 박아 넣지 않으면 이름을 추측하다 실패하고 "화면이 백지였다"고 반응한다 —
 * 자극물이 아니라 도구 한계를 평가하게 된다. 그래서 목록 생성을 여기서 한다.
 *
 * 실행:
 *   node scripts/ut/make-persona-prompt.mjs --persona P01           # 트랙 A
 *   node scripts/ut/make-persona-prompt.mjs --persona P03 --free 2  # 트랙 B 2턴
 */

import { readFileSync } from 'node:fs';
import path from 'node:path';
import { ROOT, runPaths } from './lib/paths.mjs';
import { curatedFileList, turnFileList } from './lib/filelist.mjs';

const arg = (n, d = null) => { const i = process.argv.indexOf(`--${n}`); return i > -1 ? (process.argv[i + 1] ?? d) : d; };
const out = (m) => process.stdout.write(`${m}\n`);

const personaId = arg('persona');
if (!personaId) throw new Error('--persona P01 이 필요합니다.');
const freeTurn = arg('free');
const reportOnly = process.argv.includes('--report');

if (freeTurn) {
  const runDir = path.join(ROOT, '.ut/runs', `${personaId}-free`);
  const files = turnFileList(runDir, Number(freeTurn));
  if (files.length === 0) throw new Error(`턴 ${freeTurn} 캡처가 없습니다: ${runDir}`);
  if (reportOnly) {
  out(`[이어지는 체험 — 이 지시가 네 기본 설정보다 우선한다]
persona-simulation/landing/index.html 은 읽지 마라. 예전 시안이고 지금과 무관하다.

너는 **얼마 전 이 서비스를 한 번 써 본 적이 있다.** 그때 브랜드를 등록하고, 썸네일 1장과 상세페이지 1건을 만들었다.
그때는 "진단 리포트"라는 게 있다는 건 알았지만 실제로 돌려 보지는 못했다.
**오늘 다시 들어가서, 그 진단 리포트를 처음으로 받아 봤다.**

오늘 본 화면이 아래 파일에 순서대로 들어 있다. **위에서부터 순서대로 전부 Read 해라.**
이 목록 밖의 파일은 열지 마라.

${screens.map((f) => `  ${R}/${f.trim()}`).join('\n')}

읽는 법
  .png 는 네가 실제로 본 화면, 같은 번호의 .txt 는 그 화면의 글자다(인용은 .txt 를 근거로 삼아라)
  같은 번호에 a·b·c 가 붙은 건 같은 화면을 위에서 아래로 훑어본 것이다
  리포트는 탭이 3개다 — 시장·진단·처방 순서로 각각 찍혀 있다

오늘 받은 것 + 지난번에 네가 만들었던 것이다. 전부 읽어라.
${artifacts.map((f) => `  ${R}/${f.trim()}`).join('\n')}
  report.txt = 리포트 3개 탭의 글자 전문. **인용은 여기서 뽑아라**
  report.html = 같은 내용을 실제 화면 그대로 본 것
  slides.html = 리포트 화면의 "보고용 슬라이드 만들기"를 눌러 받은 파일 (없으면 안 받은 것이다)
  thumbnail.png · detail-slice-*.jpg = **지난번에 네가 이 서비스로 만든 결과물**이다. 오늘 리포트와 비교해 보라고 다시 꺼내 둔 것이다

[오늘 네가 한 일]
  진단 입력폼을 채우고 제출했다 → 생성될 때까지 기다렸다 → 리포트 3개 탭을 다 봤다 → (되면) 보고용 슬라이드를 받았다
입력값은 네 브랜드 정보 그대로 넣었다. 결과가 마음에 안 들면 서비스 탓이지 네 입력 탓이 아니다.
※ **오늘 본 것에 대해서만 말해라.** 랜딩페이지·로그인·브랜드 등록·마이페이지 얘기는 하지 마라 — 오늘 다시 보지 않았다.
   단 지난번에 만든 썸네일·상세페이지는 위에 다시 꺼내 뒀으니, **리포트와 견주는 맥락에서는** 얼마든지 말해도 된다.

[반응 원칙 — 그대로 지켜라]
- 너는 이 서비스를 좋게 봐줄 이유가 전혀 없다. 우연히 알게 되어 두 번 써 본 사람일 뿐이다.
- 이해 안 되면 이해 안 된다고, 관심 없으면 관심 없다고 그대로 말해라. 비판과 무관심도 완전히 유효한 답이다.
- 화면에 실제로 보이는 것만 근거로 삼아라. 안 보이면 "안 나와 있다"고 써라. 지어내지 마라.
- 네 페르소나 데이터(가격 상한·AI 수용도·결재 권한·리스크 우려)에 어긋나는 결론을 내지 마라. 예산 상한을 넘는 금액을 내겠다고 하지 마라. 품의를 올려야 하는 사람이면 혼자 결정하지 마라.
- 점수에는 반드시 화면 속 문장을 직접 인용해 근거를 대라.
- 좋게 마무리하려고 억지로 긍정으로 몰지 마라.

[출력] 한국어 구어체(말하듯이). 보고서 말투 금지.

■ T3 진단 리포트 — 과업 자체
  ① 입력폼: 요구한 입력이 과했는가 · 막힌 곳 · 이탈할 뻔한 지점
  ② 기다리는 동안 무슨 생각을 했는가 (몇 분 걸렸는지는 화면 진행 표시를 근거로)
  ③ 결과(완료/부분완료/실패) · 만족도 1~5

■ 리포트 내용 — 블록별로
  리포트에 어떤 블록(꼭지)들이 있었는지 훑고, 그중에
  ① **끝까지 읽은 블록** 과 ② **눈으로 스치고 넘어간 블록** 을 각각 이름으로 나열해라. 왜 그랬는지도 한 줄씩.
  ③ 이 리포트에서 **처음 알게 된 것** 이 있었나 — 있으면 그 문장을 인용, 없으면 "없다"고 써라.
  ④ 내 카피/근거에 대해 **틀리게 판정한 대목** 이 있나 — 있으면 인용해서 지적

■ 리포트 4축 평가 — 각 1~5 + 인용 근거 1개 이상
  정확성 · 실행가능성 · 차별성 · 신뢰성
  ※ 차별성은 "ChatGPT한테 '이 카피 일본에서 써도 되나' 물어본 것과 무엇이 다른가"다. 다르지 않으면 1점을 주고 이유를 써라
  + "이 리포트대로 실제로 뭔가 고칠 것인가"(Y / 일부 Y / N)

■ 폐루프 — 이게 오늘의 핵심 질문이다
  위에 다시 꺼내 둔 **지난번 썸네일·상세페이지**를 오늘 리포트와 나란히 놓고 봐라.
  ① 리포트의 진단이 그 결과물에 **반영돼 있다고 느끼는가** (그렇다 / 일부 / 아니다) — 그렇게 본 근거를 산출물이나 리포트에서 인용해라
  ② 만약 **리포트를 먼저 받고 나서** 썸네일·상세페이지를 만들었다면 결과가 달라졌을 것 같은가
  ③ 리포트와 제작물이 **한 서비스로 이어진 것처럼** 보이는가, 아니면 **따로 노는 두 도구**처럼 보이는가

■ 보고용 슬라이드 (slides.html 을 받았다면)
  ① 이걸로 사내 보고를 할 수 있겠는가 · ② 빠져서 못 쓰겠는 것 하나
  ※ 결재 권한이 없어 품의를 올려야 하는 사람이면 이 항목을 특히 구체적으로 답해라. 못 받았으면 "못 받았다"고만 써라.

■ 세 산출물 비교 — 리포트 / 썸네일 / 상세페이지
  이제 셋을 다 받아 봤다. **가장 돈값 하는 것 하나**를 고르고 이유를 대라.

■ 지불 의향 — 다시 묻는다
  리포트까지 보고 난 지금, 낼 것인가(예/아니오) · 금액 · NPS 0~10 · 다시 올 것인가
  ※ 지난번 답과 달라졌다면 **무엇 때문에 달라졌는지** 한 줄로 밝혀라. 안 달라졌으면 그대로 둬라.

■ 마지막 3문장: 오늘 가장 좋았던 것 / 가장 나빴던 것 / 이게 없어서 못 사겠다 싶은 것 하나

■ 맨 끝에 아래 블록을 **정확히 이 형식으로** 붙여라. 위에서 말한 것과 숫자가 달라선 안 된다.
  집계가 이 블록만 읽는다 — 여기 없는 값은 세상에 없는 값이 된다. 해당 없으면 빈칸으로 두고 줄은 지우지 마라.

\`\`\`scores
sat_T3=              # 리포트 과업 만족도 1~5
dropoff_T3=          # 이탈할 뻔한 화면 이름. 없으면 빈칸
rep_acc=  rep_act=  rep_dif=  rep_tru=   # 리포트 정확성·실행가능성·차별성·신뢰성 1~5
rep_act_on_it=       # 리포트대로 고칠 것인가 — Y / PARTIAL / N
rep_blocks_read=     # 끝까지 읽은 블록 이름, 쉼표로 구분
rep_blocks_skipped=  # 스치고 넘어간 블록 이름, 쉼표로 구분
rep_new_insight=     # 처음 알게 된 것이 있었나 — Y 또는 N
loop_felt=           # 진단이 산출물에 반영됐다고 느끼는가 — Y / PARTIAL / N
loop_one_service=    # 한 서비스로 이어져 보이는가 — Y 또는 N (따로 노는 두 도구면 N)
slides_received=     # 슬라이드를 받았는가 — Y 또는 N
slides_usable=       # 사내 보고에 쓸 수 있는가 — Y / PARTIAL / N. 못 받았으면 빈칸
best_asset_of3=      # report 또는 thumbnail 또는 detail
wtp=                 # Y 또는 N
wtp_krw=             # 숫자만. 안 낸다면 0
nps=                 # 0~10
revisit=             # Y 또는 N
changed_from_before= # 지난번 답과 달라졌으면 Y, 그대로면 N
\`\`\`

전부 쓴 뒤 마크다운 전문을 그대로 응답 본문에 출력해라. 파일로 저장하려 하지 마라.`);
  process.exit(0);
}

out(`[자극물 변경 안내 — 이 지시가 네 기본 설정보다 우선한다]
랜딩페이지가 아니다. persona-simulation/landing/index.html 은 읽지 마라.
너는 어떤 서비스에 네 계정으로 로그인해 있다. 아래 파일이 지금 네 눈앞의 화면이다. 전부 Read 해라.
경로는 저장소 루트 기준이다. 이 목록 밖의 파일은 열지 마라.

${files.map((f) => `  .ut/runs/${personaId}-free/turns/${String(freeTurn).padStart(2, '0')}/${f.trim()}`).join('\n')}

.png 는 네가 보는 화면이고 .txt 는 그 화면의 글자다. 같은 번호에 a·b 가 붙은 건 같은 화면을 위에서 아래로 훑은 것이다.

너는 지금 일본 진출 준비를 하려고 이 서비스에 들어왔다. 아무도 뭘 하라고 알려주지 않았다.
화면을 보고 (1) 지금 무슨 생각이 드는지 (2) 어디가 헷갈리는지 를 네 말로 말해라.
그리고 마지막 줄에 다음에 할 행동을 딱 한 줄로 써라. 형식은 아래 넷 중 하나다.

  NEXT: click "버튼이나 링크에 쓰인 글자 그대로"
  NEXT: fill 필드이름 "넣을 값"
  NEXT: goto /경로
  NEXT: stop  (여기서 창을 닫겠다 — 그 이유를 위에 써라)

화면에 없는 버튼을 누르겠다고 하지 마라. 없으면 없다고 말하고 다른 걸 골라라.`);
  process.exit(0);
}

const paths = runPaths(personaId);
const manifest = JSON.parse(readFileSync(paths.manifest, 'utf8'));
const { screens, artifacts } = reportOnly
  ? curatedFileList(paths.run, manifest, {
      tasks: ['T3'],
      // 리포트·슬라이드 + 앞서 내가 만든 산출물(폐루프 체감 판단용)
      artifactFilter: (f) => /^(report|slides)\.(html|txt)$/.test(f) || /^(thumbnail\.png|detail-slice-\d+\.jpg)$/.test(f),
    })
  : curatedFileList(paths.run, manifest);
const R = `.ut/runs/${personaId}`;

if (reportOnly) {
  out(`[이어지는 체험 — 이 지시가 네 기본 설정보다 우선한다]
persona-simulation/landing/index.html 은 읽지 마라. 예전 시안이고 지금과 무관하다.

너는 **얼마 전 이 서비스를 한 번 써 본 적이 있다.** 그때 브랜드를 등록하고, 썸네일 1장과 상세페이지 1건을 만들었다.
그때는 "진단 리포트"라는 게 있다는 건 알았지만 실제로 돌려 보지는 못했다.
**오늘 다시 들어가서, 그 진단 리포트를 처음으로 받아 봤다.**

오늘 본 화면이 아래 파일에 순서대로 들어 있다. **위에서부터 순서대로 전부 Read 해라.**
이 목록 밖의 파일은 열지 마라.

${screens.map((f) => `  ${R}/${f.trim()}`).join('\n')}

읽는 법
  .png 는 네가 실제로 본 화면, 같은 번호의 .txt 는 그 화면의 글자다(인용은 .txt 를 근거로 삼아라)
  같은 번호에 a·b·c 가 붙은 건 같은 화면을 위에서 아래로 훑어본 것이다
  리포트는 탭이 3개다 — 시장·진단·처방 순서로 각각 찍혀 있다

오늘 받은 것 + 지난번에 네가 만들었던 것이다. 전부 읽어라.
${artifacts.map((f) => `  ${R}/${f.trim()}`).join('\n')}
  report.txt = 리포트 3개 탭의 글자 전문. **인용은 여기서 뽑아라**
  report.html = 같은 내용을 실제 화면 그대로 본 것
  slides.html = 리포트 화면의 "보고용 슬라이드 만들기"를 눌러 받은 파일 (없으면 안 받은 것이다)
  thumbnail.png · detail-slice-*.jpg = **지난번에 네가 이 서비스로 만든 결과물**이다. 오늘 리포트와 비교해 보라고 다시 꺼내 둔 것이다

[오늘 네가 한 일]
  진단 입력폼을 채우고 제출했다 → 생성될 때까지 기다렸다 → 리포트 3개 탭을 다 봤다 → (되면) 보고용 슬라이드를 받았다
입력값은 네 브랜드 정보 그대로 넣었다. 결과가 마음에 안 들면 서비스 탓이지 네 입력 탓이 아니다.
※ **오늘 본 것에 대해서만 말해라.** 랜딩페이지·로그인·브랜드 등록·마이페이지 얘기는 하지 마라 — 오늘 다시 보지 않았다.
   단 지난번에 만든 썸네일·상세페이지는 위에 다시 꺼내 뒀으니, **리포트와 견주는 맥락에서는** 얼마든지 말해도 된다.

[반응 원칙 — 그대로 지켜라]
- 너는 이 서비스를 좋게 봐줄 이유가 전혀 없다. 우연히 알게 되어 두 번 써 본 사람일 뿐이다.
- 이해 안 되면 이해 안 된다고, 관심 없으면 관심 없다고 그대로 말해라. 비판과 무관심도 완전히 유효한 답이다.
- 화면에 실제로 보이는 것만 근거로 삼아라. 안 보이면 "안 나와 있다"고 써라. 지어내지 마라.
- 네 페르소나 데이터(가격 상한·AI 수용도·결재 권한·리스크 우려)에 어긋나는 결론을 내지 마라. 예산 상한을 넘는 금액을 내겠다고 하지 마라. 품의를 올려야 하는 사람이면 혼자 결정하지 마라.
- 점수에는 반드시 화면 속 문장을 직접 인용해 근거를 대라.
- 좋게 마무리하려고 억지로 긍정으로 몰지 마라.

[출력] 한국어 구어체(말하듯이). 보고서 말투 금지.

■ T3 진단 리포트 — 과업 자체
  ① 입력폼: 요구한 입력이 과했는가 · 막힌 곳 · 이탈할 뻔한 지점
  ② 기다리는 동안 무슨 생각을 했는가 (몇 분 걸렸는지는 화면 진행 표시를 근거로)
  ③ 결과(완료/부분완료/실패) · 만족도 1~5

■ 리포트 내용 — 블록별로
  리포트에 어떤 블록(꼭지)들이 있었는지 훑고, 그중에
  ① **끝까지 읽은 블록** 과 ② **눈으로 스치고 넘어간 블록** 을 각각 이름으로 나열해라. 왜 그랬는지도 한 줄씩.
  ③ 이 리포트에서 **처음 알게 된 것** 이 있었나 — 있으면 그 문장을 인용, 없으면 "없다"고 써라.
  ④ 내 카피/근거에 대해 **틀리게 판정한 대목** 이 있나 — 있으면 인용해서 지적

■ 리포트 4축 평가 — 각 1~5 + 인용 근거 1개 이상
  정확성 · 실행가능성 · 차별성 · 신뢰성
  ※ 차별성은 "ChatGPT한테 '이 카피 일본에서 써도 되나' 물어본 것과 무엇이 다른가"다. 다르지 않으면 1점을 주고 이유를 써라
  + "이 리포트대로 실제로 뭔가 고칠 것인가"(Y / 일부 Y / N)

■ 폐루프 — 이게 오늘의 핵심 질문이다
  위에 다시 꺼내 둔 **지난번 썸네일·상세페이지**를 오늘 리포트와 나란히 놓고 봐라.
  ① 리포트의 진단이 그 결과물에 **반영돼 있다고 느끼는가** (그렇다 / 일부 / 아니다) — 그렇게 본 근거를 산출물이나 리포트에서 인용해라
  ② 만약 **리포트를 먼저 받고 나서** 썸네일·상세페이지를 만들었다면 결과가 달라졌을 것 같은가
  ③ 리포트와 제작물이 **한 서비스로 이어진 것처럼** 보이는가, 아니면 **따로 노는 두 도구**처럼 보이는가

■ 보고용 슬라이드 (slides.html 을 받았다면)
  ① 이걸로 사내 보고를 할 수 있겠는가 · ② 빠져서 못 쓰겠는 것 하나
  ※ 결재 권한이 없어 품의를 올려야 하는 사람이면 이 항목을 특히 구체적으로 답해라. 못 받았으면 "못 받았다"고만 써라.

■ 세 산출물 비교 — 리포트 / 썸네일 / 상세페이지
  이제 셋을 다 받아 봤다. **가장 돈값 하는 것 하나**를 고르고 이유를 대라.

■ 지불 의향 — 다시 묻는다
  리포트까지 보고 난 지금, 낼 것인가(예/아니오) · 금액 · NPS 0~10 · 다시 올 것인가
  ※ 지난번 답과 달라졌다면 **무엇 때문에 달라졌는지** 한 줄로 밝혀라. 안 달라졌으면 그대로 둬라.

■ 마지막 3문장: 오늘 가장 좋았던 것 / 가장 나빴던 것 / 이게 없어서 못 사겠다 싶은 것 하나

■ 맨 끝에 아래 블록을 **정확히 이 형식으로** 붙여라. 위에서 말한 것과 숫자가 달라선 안 된다.
  집계가 이 블록만 읽는다 — 여기 없는 값은 세상에 없는 값이 된다. 해당 없으면 빈칸으로 두고 줄은 지우지 마라.

\`\`\`scores
sat_T3=              # 리포트 과업 만족도 1~5
dropoff_T3=          # 이탈할 뻔한 화면 이름. 없으면 빈칸
rep_acc=  rep_act=  rep_dif=  rep_tru=   # 리포트 정확성·실행가능성·차별성·신뢰성 1~5
rep_act_on_it=       # 리포트대로 고칠 것인가 — Y / PARTIAL / N
rep_blocks_read=     # 끝까지 읽은 블록 이름, 쉼표로 구분
rep_blocks_skipped=  # 스치고 넘어간 블록 이름, 쉼표로 구분
rep_new_insight=     # 처음 알게 된 것이 있었나 — Y 또는 N
loop_felt=           # 진단이 산출물에 반영됐다고 느끼는가 — Y / PARTIAL / N
loop_one_service=    # 한 서비스로 이어져 보이는가 — Y 또는 N (따로 노는 두 도구면 N)
slides_received=     # 슬라이드를 받았는가 — Y 또는 N
slides_usable=       # 사내 보고에 쓸 수 있는가 — Y / PARTIAL / N. 못 받았으면 빈칸
best_asset_of3=      # report 또는 thumbnail 또는 detail
wtp=                 # Y 또는 N
wtp_krw=             # 숫자만. 안 낸다면 0
nps=                 # 0~10
revisit=             # Y 또는 N
changed_from_before= # 지난번 답과 달라졌으면 Y, 그대로면 N
\`\`\`

전부 쓴 뒤 마크다운 전문을 그대로 응답 본문에 출력해라. 파일로 저장하려 하지 마라.`);
  process.exit(0);
}

out(`[자극물 변경 안내 — 이 지시가 네 기본 설정보다 우선한다]
persona-simulation/landing/index.html 은 읽지 마라. 그건 예전에 봤던 시안 파일이고 지금과 무관하다.
너는 오늘 어떤 웹사이트를 우연히 알게 되어 직접 들어가 봤고, 계정을 만들어 끝까지 써 봤다.

네가 본 화면이 아래 파일에 순서대로 들어 있다. **위에서부터 순서대로 전부 Read 해라.**
경로는 저장소 루트 기준 그대로다. 이 목록 밖의 파일은 열지 마라 — 다른 사람의 폴더, 기획 문서, 코드는 네가 볼 수 없는 것이다.

${screens.map((f) => `  ${R}/${f.trim()}`).join('\n')}

읽는 법
  .png 는 네가 실제로 본 화면이고, 같은 번호의 .txt 는 그 화면의 글자다(인용은 .txt 를 근거로 삼아라)
  같은 번호에 a·b·c 가 붙은 건 **같은 화면을 위에서 아래로 훑어본 것**이다
  00 으로 시작하는 것 = 로그인 전에 본 **랜딩페이지**. 위에서 아래로 스크롤한 순서다
  01 부터 = 로그인한 뒤 서비스 안에서 본 화면들

네가 받은 결과물이다. 이것도 전부 읽어라.
${artifacts.map((f) => `  ${R}/${f.trim()}`).join('\n')}
  detail-slice-01.jpg, -02.jpg … 가 **상세페이지 본체**다. 실제 구매자도 이렇게 조각으로 스크롤하며 본다
  detail-master.jpg 는 그 조각을 세로로 다 이어붙인 것 — 전체 길이와 흐름만 확인하는 용도다(글자는 조각에서 읽어라)

[네가 한 일]
  T0 랜딩페이지를 처음부터 끝까지 (혹은 도중까지) 스크롤해서 봤다 — 아직 로그인 전이다
  T1 로그인하고 첫 화면까지 갔다
  T2 네 브랜드를 등록했다
  T4 제품 사진을 올려 썸네일을 1장 만들었다
  T5 상세페이지를 1건 만들었다
  T6 자산 라이브러리·시즌 캘린더·기업 매칭 화면을 둘러봤다
  T7 마이페이지까지 보고 나왔다
※ 이번에 진단 리포트는 만들지 않았다. 화면에서 진단 얘기를 봤더라도 너는 그걸 써 보지 않았으니,
   리포트 내용에 대해서는 아무 말도 하지 마라. "그런 게 있다더라" 수준의 인상만 말할 수 있다.
입력값은 네 브랜드 정보 그대로 넣었다. 결과가 마음에 안 들면 그건 서비스 탓이지 네 입력 탓이 아니다.
※ 제품 사진은 라벨 없는 무지 용기다. "내 제품이 아닌 것 같다"는 느낌이 들면 그건 사진 탓이니 결과물 평가에서는 빼고 말해라.

[반응 원칙 — 그대로 지켜라]
- 너는 이 서비스를 좋게 봐줄 이유가 전혀 없다. 우연히 알게 되어 한 번 써 본 사람일 뿐이다.
- 이해 안 되면 이해 안 된다고, 관심 없으면 관심 없다고 그대로 말해라. 비판과 무관심도 완전히 유효한 답이다.
- 화면에 실제로 보이는 것만 근거로 삼아라. 안 보이는 기능을 있다고 가정하거나 지어내지 마라. 안 나와 있으면 "안 나와 있다"고 써라.
- 네 페르소나 데이터(가격 상한·AI 수용도·결재 권한·리스크 우려)에 어긋나는 결론을 내지 마라. 네 예산 상한을 넘는 금액을 내겠다고 하지 마라. 네가 품의를 올려야 하는 사람이면 혼자 결정하지 마라.
- 점수를 매길 때는 반드시 화면 속 문장이나 요소를 직접 인용해서 근거를 대라. 근거 없는 점수는 쓰지 마라.
- 좋게 마무리하려고 억지로 긍정으로 몰지 마라.

[출력] 한국어 구어체(말하듯이). 보고서 말투 금지.

■ T0 랜딩페이지 — 아래 순서로
  ① 3초 첫인상 — 스크롤하기 전, 맨 위 화면만 보고 든 생각
  ② 이게 뭘 해준다는 서비스인지 네 말로 설명
  ③ 네 상황과 맞는 얘기인가
  ④ 로그인하거나 신청까지 갈 마음이 드는가 — 5점 만점으로
  ⑤ 어느 지점에서 창을 닫았을 것 같은가
  + 매력도 1~5 (보기 좋고 믿음이 가는가)
  + 흥미도 1~5 (더 읽고 싶어지는가)
  + 끝까지 스크롤했는가, 아니면 어디서 멈췄는가
  + 가장 눈에 걸린 문장 1개 / 가장 걸리적거린 문장 1개 — 화면에 있는 그대로 인용

■ T1·T2·T4·T5·T6·T7 각각: 결과(완료/부분완료/실패) · 만족도 1~5 · 눈에 걸린 것 · 그냥 지나친 것 · 막힌 곳 · 이탈할 뻔한 지점
  T2 추가: 요구하는 입력이 과했는지 · 포지셔닝 태그 고르기가 어땠는지
  T4 추가: 템플릿을 왜 그걸 골랐는지 · 가격 자리가 비어 있는 걸 어떻게 봤는지 · 기다리는 동안 무슨 생각을 했는지
  T5 추가: 내용량·구분·판매원을 반드시 넣으라고 요구받았을 때 기분 · 3분 가까운 대기 · 빠진 블록 안내를 어떻게 읽었는지
  T6 추가: 방금 만든 것 2개(썸네일·상세페이지)가 제대로 보였는지 · "기업 매칭"이 지금 되는 서비스로 보였는지 나중에 될 거로 보였는지
  T7 추가: 이 서비스를 남에게 한 문장으로 어떻게 설명할지 (네 말로)

■ 산출물 평가 — 썸네일 / 상세페이지 2개 각각에 대해
  정확성 · 실행가능성 · 차별성 · 신뢰성 각 1~5 + 인용 근거 1개 이상 + "그대로 쓸 것인가"(Y / 손보면 Y / N)
  ※ 차별성은 "번역기나 ChatGPT, 흔한 이미지 생성툴로 뽑은 것과 무엇이 다른가"다. 다르지 않으면 1점을 주고 이유를 써라
■ 틀렸다고 생각한 대목: 사실이 틀렸거나 네 브랜드와 안 맞는 문장을 직접 인용해서 지적
■ 지불 의향: 낼 것인가(예/아니오) · 금액 · 둘 중 가장 돈값 하는 것 하나 · 이유 · NPS 0~10 · 다시 올 것인가
■ 마지막 3문장: 가장 좋았던 것 / 가장 나빴던 것 / 이게 없어서 못 사겠다 싶은 것 하나

■ 맨 끝에 아래 블록을 **정확히 이 형식으로** 붙여라. 위에서 네가 말한 것과 숫자가 달라선 안 된다.
  집계가 이 블록만 읽는다 — 여기 없는 값은 세상에 없는 값이 된다. 해당 없으면 빈칸으로 두고 줄은 지우지 마라.

\`\`\`scores
landing_appeal=      # 매력도 1~5
landing_interest=    # 흥미도 1~5
landing_intent=      # 다음 행동 의향 1~5
landing_scrolled_to_end=   # Y 또는 N
landing_exit_section=      # 멈춘 섹션 이름(한 단어). 끝까지 봤으면 빈칸
landing_hook_quote=        # 가장 눈에 걸린 문장, 화면 원문 그대로 한 줄
landing_friction_quote=    # 가장 걸리적거린 문장, 화면 원문 그대로 한 줄
sat_T1=  sat_T2=  sat_T4=  sat_T5=  sat_T6=  sat_T7=   # 과업 만족도 1~5
dropoff_T1=  dropoff_T2=  dropoff_T4=  dropoff_T5=  dropoff_T6=  dropoff_T7=   # 이탈할 뻔한 화면 이름. 없으면 빈칸
thu_acc=  thu_act=  thu_dif=  thu_tru=    # 썸네일 정확성·실행가능성·차별성·신뢰성 1~5
det_acc=  det_act=  det_dif=  det_tru=    # 상세페이지 4축 1~5
best_asset=          # thumbnail 또는 detail
wtp=                 # Y 또는 N
wtp_krw=             # 숫자만. 안 낸다면 0
nps=                 # 0~10
revisit=             # Y 또는 N
\`\`\`

전부 쓴 뒤 마크다운 전문을 그대로 응답 본문에 출력해라. 파일로 저장하려 하지 마라.`);
